// Minimal placeholder config to avoid TypeScript tooling scanning errors.
// The real Tailwind config used by the app lives in my-showcase/tailwind.config.ts
const config = {
  content: [],
  theme: {},
  plugins: [],
};

export default config;
